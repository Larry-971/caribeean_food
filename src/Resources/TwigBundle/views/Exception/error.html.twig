<!DOCTYPE html>
<html>
    <head>
        <meta charset="{{ _charset }}" />
        <title>Une erreur c'est produite Générique{{ status_text }}</title>
        <style>
            body { background-color: #fff; color: #222; font: 16px/1.5 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif; margin: 0; }
            .container { margin: 30px; max-width: 600px; }
            h1 { color: #dc3545; font-size: 24px; }
            h2 { font-size: 18px; }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>Oops!Erreur Générique ! Raté ce n'est pas la bonne URL</h1>
            <h2>Le serveur à retourné le code  "{{ status_code }} {{ status_text }}".</h2>

            <p>
               Quelque chose est cassé
            </p>
        </div>
    </body>
</html>