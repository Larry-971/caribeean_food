{% extends 'base_back.html.twig' %}

{% block title %}Edit Plat{% endblock %}

{% block body %}
    <h1>Edit Plat</h1>

    {{ include('plat/_form.html.twig', {'button_label': 'Mise à jour'}) }}

    <a href="{{ path('index_plat') }}">back to list</a>

    {{ include('plat/_delete_form.html.twig') }}
{% endblock %}
