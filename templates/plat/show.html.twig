{% extends 'base_back.html.twig' %}

{% block title %}Plat{% endblock %}

{% block body %}
    <h1>Plat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plat.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ plat.Nom }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ plat.Photo }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ plat.Description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ plat.Prix }}</td>
            </tr>
            <tr>
                <th>Publication</th>
                <td>{{ plat.Publication }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('index_plat') }}">back to list</a>

    <a href="{{ path('edit_plat', {'id': plat.id}) }}">edit</a>

    {{ include('plat/_delete_form.html.twig') }}
{% endblock %}
