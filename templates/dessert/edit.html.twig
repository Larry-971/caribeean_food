{% extends 'base_back.html.twig' %}


{% block title %}Modification du dessert{% endblock %}

{% block body %}
    <a href="{{ path('index_dessert') }}"><i class="fas fa-arrow-circle-left fa-3x"></i></a> 
    <br><br>

    <h1>Modification du dessert : {{ dessert.Nom }}</h1>
    <br>
    <img  class="img-thumbnail mx-auto d-block" src="{{ asset('uploads/' ~ dessert.Photo) }} " alt="image_dessert" width="200" height="100"> 
    {# Champs hidden pour validé photo #}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_row(form.Photo) }}
    {{ form_end(form) }}
    {# {{ include('plat/_delete_form.html.twig') }} #}
<style>
.container{
    background: #dcdcdc73;
}
</style>
{% endblock %}
