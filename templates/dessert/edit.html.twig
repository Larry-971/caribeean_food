{% extends 'base_back.html.twig' %}


{% block title %}Modification du dessert{% endblock %}

{% block body %}
    <a href="{{ path('index_dessert') }}"><i class="fas fa-arrow-circle-left fa-3x"></i></a> 
    <br><br>

    <h1>Modification du dessert : {{ dessert.Nom }}</h1>
    <br>
    <img  class="img-thumbnail mx-auto d-block" src="{{ asset('uploads/' ~ dessert.Photo) }} " alt="image" width="200" height="100"> 
    {# Champs hidden pour validé photo #}
    <input type="hidden" id="dessert_Photo" name="Photo" maxlength="255" class="form-control" value="{{ dessert.Photo }}">
    {{ include('dessert/_form.html.twig') }}
    {{ include('dessert/_delete_form.html.twig') }}
{% endblock %}
